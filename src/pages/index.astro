---
import BaseLayout from '../layouts/BaseLayout.astro';
import EventRow from '../components/EventRow.astro';
import FilterBar from '../components/FilterBar.astro';
import EventMap from '../components/EventMap.astro';
import MiniCalendar from '../components/MiniCalendar.astro';
import {
  getUpcomingEvents,
  getCommunityMap,
  getCommunities,
  groupByDay,
  eventsPerDay,
  eventCount,
} from '../lib/events';
import { ROUTES } from '../lib/site';
import Message from '../components/Message.astro';
import * as m from '../paraglide/messages.js';

const now = new Date();
const events = await getUpcomingEvents(now);
const communities = await getCommunityMap();
const allCommunities = await getCommunities();

const days = groupByDay(events, now);
const counts = eventsPerDay(events);
const availableCategories = [...new Set(events.flatMap((event) => event.categories))];

// The running index ties a row in the list to its pin on the map.
let pin = 0;
const numbered = new Map(events.map((event) => [event.id, ++pin]));

const description =
  events.length > 0
    ? m.home_metaDescriptionCount({ count: events.length })
    : m.home_metaDescription();

const communityCount = m.communities_count({ count: allCommunities.length });
---

<BaseLayout title={m.home_title()} description={description}>
  <section class="container hero">
    <h1>{m.home_headline()}<br /><span>{m.home_headlineAccent()}</span></h1>
    <p class="lede">{m.home_intro()}</p>
  </section>

  <div class="container">
    <FilterBar availableCategories={availableCategories} />
  </div>

  <div class="container layout">
    <div class="agenda">
      {
        days.map((day) => (
          <section class="day" data-day-group={day.key}>
            <header class="day-head">
              <h2 class="day-label">{day.label}</h2>
              <span class="eyebrow" data-day-count>
                {eventCount(day.events.length)}
              </span>
            </header>

            {day.events.map((event) => (
              <EventRow
                event={event}
                community={communities.get(event.communityId)}
                index={numbered.get(event.id) ?? 0}
              />
            ))}
          </section>
        ))
      }

      <p class="empty" data-empty-state hidden={events.length > 0}>
        {
          events.length === 0 ? (
            <Message of={m.home_empty} links={{ submit: ROUTES.submit }} />
          ) : (
            m.home_emptyFiltered()
          )
        }
      </p>
    </div>

    <aside class="side">
      <EventMap />
      <MiniCalendar counts={counts} />

      <section class="promo">
        <h2>{communityCount}</h2>
        <p>{m.home_communitiesCta()}</p>
        <a class="btn btn--primary" href={ROUTES.communities}>{m.home_communitiesLink()}</a>
      </section>
    </aside>
  </div>
</BaseLayout>

<style>
  .hero {
    padding-block: var(--space-7) var(--space-5);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .hero h1 span {
    color: var(--accent);
  }

  .layout {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 360px;
    gap: var(--space-6);
    align-items: start;
    padding-block: var(--space-5);
  }

  .day + .day {
    margin-top: var(--space-5);
  }

  .day[hidden] {
    display: none;
  }

  .day-head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: var(--space-3);
    padding-bottom: var(--space-2);
    border-bottom: 2px solid var(--ink);
  }

  .day-label {
    font-size: var(--step-0);
    font-weight: 700;
    letter-spacing: 0;
    min-width: 0; /* Without this a long title squeezes the counter out. */
  }

  .day-head .eyebrow {
    flex-shrink: 0;
  }

  .empty {
    padding: var(--space-6) 0;
    color: var(--ink-2);
    max-width: var(--measure);
  }

  .side {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    position: sticky;
    top: 84px;
  }

  .promo {
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    background: var(--surface);
    padding: var(--space-4);
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    align-items: flex-start;
  }

  .promo h2 {
    font-size: var(--step-2);
  }

  .promo p {
    font-size: var(--step--1);
    color: var(--ink-2);
  }

  .promo .btn {
    margin-top: var(--space-2);
  }

  @media (max-width: 1000px) {
    .layout {
      grid-template-columns: 1fr;
    }
    /* On narrow screens the list comes first: events are why people open the
       site, the map and the calendar are context. */
    .side {
      position: static;
    }
  }
</style>
